(function(){
  if(window.__rrtaManagerTrainingRequestLoaded)return;window.__rrtaManagerTrainingRequestLoaded=true;
  if((location.pathname.split('/').pop()||'').toLowerCase()!=='manager-member.html')return;
  const SUPA='https://qgbpotjqggeodxqcwkgj.supabase.co',KEY='sb_publishable_J1yPM1Hi7INCX2m7rp3PdA_JdQ46FRS';
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const memberId=new URLSearchParams(location.search).get('id'); let db,member;
  const style=document.createElement('style');style.textContent=`.mgr-course-modal{position:fixed;inset:0;z-index:2147483601;background:rgba(15,20,27,.62);display:grid;place-items:center;padding:20px;backdrop-filter:blur(2px)}.mgr-course-panel{width:min(760px,96vw);max-height:92vh;overflow:auto;background:#fff;border-radius:16px;box-shadow:0 28px 75px rgba(0,0,0,.3)}.mgr-course-head{display:flex;justify-content:space-between;gap:16px;padding:20px;border-bottom:1px solid #e4e7eb}.mgr-course-head h2{margin:5px 0 0}.mgr-course-close{width:34px;height:34px;border:0;border-radius:50%;background:#eef1f4;font-size:20px;cursor:pointer}.mgr-course-body{padding:20px}.mgr-course-intro{padding:12px 14px;border-radius:10px;background:#f7f8fa;border:1px solid #e3e7eb;margin-bottom:14px}.mgr-course-intro strong{display:block;font-size:12px}.mgr-course-intro span{display:block;font-size:10px;color:#6f7a88;margin-top:4px}.mgr-course-list{display:grid;gap:10px}.mgr-course-option{border:1px solid #e1e5ea;border-radius:12px;padding:14px;display:grid;grid-template-columns:1fr auto;gap:14px;align-items:center;background:#fff}.mgr-course-option:first-child{border-color:#d8a4ac;box-shadow:inset 4px 0 0 #b20f22}.mgr-course-option h3{margin:0 0 5px;font-size:13px}.mgr-course-option p{margin:2px 0;font-size:10px;color:#6f7a88}.mgr-course-option button,.mgr-no-date{border:0;border-radius:8px;padding:9px 11px;font-size:9px;font-weight:850;cursor:pointer}.mgr-course-option button{background:#b20f22;color:#fff}.mgr-no-date{margin-top:14px;background:#fff;color:#20262d;border:1px solid #d5dae1}.mgr-course-message{margin-top:12px;padding:11px 12px;border-radius:9px;background:#eef6f0;color:#176b37;font-size:10px;font-weight:800}.mgr-book-request{border:0;background:#b20f22;color:#fff;border-radius:8px;padding:8px 9px;font-size:9px;font-weight:850;cursor:pointer}.mgr-book-request:hover{background:#8f0c1b}@media(max-width:650px){.mgr-course-option{grid-template-columns:1fr}}`;document.head.appendChild(style);
  const fmt=d=>d?new Date(d+'T12:00:00').toLocaleDateString('en-GB',{weekday:'short',day:'2-digit',month:'short',year:'numeric'}):'—';
  async function submit(requirement,scheduleId,response,modal){const msg=modal.querySelector('[data-course-msg]');msg.className='mgr-course-message';msg.textContent='Sending request to the Training Academy…';const {data,error}=await db.rpc('submit_manager_training_request',{p_learner_id:memberId,p_requirement:requirement,p_schedule_id:scheduleId||null,p_availability_response:response||null});if(error){msg.style.background='#fff1f2';msg.style.color='#b20f22';msg.textContent=error.message;return}msg.textContent=scheduleId?'Date requested. The Academy can now confirm the place.':'Request left pending. The Academy can arrange another suitable date.';modal.querySelectorAll('button').forEach(b=>b.disabled=true);setTimeout(()=>modal.remove(),1800)}
  async function open(requirement){document.querySelector('.mgr-course-modal')?.remove();const modal=document.createElement('div');modal.className='mgr-course-modal';modal.innerHTML=`<section class="mgr-course-panel"><div class="mgr-course-head"><div><div class="eyebrow">Training Required</div><h2>${esc(requirement)}</h2></div><button class="mgr-course-close" type="button">×</button></div><div class="mgr-course-body"><div class="mgr-course-intro"><strong>Choose a suitable course for ${esc(member?.full_name||'this operative')}</strong><span>Available RRTA dates are checked against the live Training Schedule. Choosing a date sends a request to the Academy for confirmation.</span></div><div class="mgr-course-list"><div class="empty">Checking upcoming courses…</div></div><button class="mgr-no-date" type="button" data-no-date>None of these dates work</button><div data-course-msg></div></div></section>`;document.body.appendChild(modal);modal.querySelector('.mgr-course-close').onclick=()=>modal.remove();modal.addEventListener('click',e=>{if(e.target===modal)modal.remove()});const {data,error}=await db.rpc('manager_training_options',{p_learner_id:memberId,p_requirement:requirement});const host=modal.querySelector('.mgr-course-list');if(error){host.innerHTML=`<div class="empty">${esc(error.message)}</div>`}else if(!data?.length){host.innerHTML='<div class="empty">No matching RRTA course is currently scheduled. Leave this as a pending request and the Academy can arrange a suitable course.</div>'}else{host.innerHTML=data.map((x,i)=>`<article class="mgr-course-option"><div><h3>${i===0?'Next available · ':''}${esc(x.course_title)}</h3><p><strong>${fmt(x.start_date)}${x.end_date&&x.end_date!==x.start_date?' – '+fmt(x.end_date):''}</strong> · ${esc(x.venue||'Venue TBC')}</p><p>${x.spaces_available==null?'Capacity managed by Academy':x.spaces_available+' space'+(x.spaces_available===1?'':'s')+' currently available'}</p></div><button type="button" data-schedule="${x.id}">Request this date</button></article>`).join('');host.querySelectorAll('[data-schedule]').forEach(b=>b.onclick=()=>submit(requirement,b.dataset.schedule,'preferred_scheduled_date',modal))}modal.querySelector('[data-no-date]').onclick=()=>submit(requirement,null,'none_of_these_dates_work',modal)}
  function wire(){document.querySelectorAll('#requirements .req').forEach(row=>{const title=row.querySelector('strong');if(!title)return;const last=row.lastElementChild;if(!last)return;const existing=[...last.querySelectorAll('a,button')].find(x=>/request training|book.*training/i.test(x.textContent));if(!existing||existing.dataset.courseFlow)return;existing.dataset.courseFlow='1';existing.textContent='Book / Request Training';existing.classList.add('mgr-book-request');if(existing.tagName==='A'){existing.removeAttribute('href');existing.setAttribute('role','button')}existing.onclick=e=>{e.preventDefault();open(title.textContent.trim())}})}
  async function boot(n=0){if(!window.supabase?.createClient){if(n<30)setTimeout(()=>boot(n+1),200);return}db=window.supabase.createClient(SUPA,KEY);const {data:u}=await db.auth.getUser();if(!u?.user||!memberId)return;const {data:m}=await db.from('profiles').select('id,full_name').eq('id',memberId).single();member=m;wire();new MutationObserver(()=>requestAnimationFrame(wire)).observe(document.body,{childList:true,subtree:true});setTimeout(wire,700)}boot();
})();