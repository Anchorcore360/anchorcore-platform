(function(){function patch(){if(!location.pathname.endsWith('/online-learning.html'))return;document.querySelectorAll('.course').forEach(card=>{const title=(card.querySelector('h3')?.textContent||'').trim();if(title!=='Customer Service & Professional Conduct')return;const provider=card.querySelector('.provider');if(provider)provider.textContent='RRTA · ONLINE ASSESSMENT';const link=card.querySelector('a.btn-primary');if(link){link.href='customer-service-online.html';link.textContent=link.textContent.includes('Review')?'Review Course':link.textContent.includes('Continue')?'Continue Course':'Start Course';}})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{setTimeout(patch,400);setTimeout(patch,1200)});else{setTimeout(patch,400);setTimeout(patch,1200)}window.addEventListener('focus',()=>setTimeout(patch,300));})();