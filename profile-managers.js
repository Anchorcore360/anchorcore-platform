let managerDirectory=[];
let jobRoleDirectory=[];
let learnerJobRoles=[];
let academyRoles=[];
const originalLoadData=loadData;
loadData=async function(){
  await originalLoadData();
  const [peopleRes,rolesRes,linksRes,academyRes]=await Promise.all([
    db.from('profiles').select('id,full_name,forename,surname,employee_number,job_title,role,account_status').order('full_name'),
    db.from('job_roles').select('id,division,name,active').eq('active',true).order('division').order('name'),
    db.from('profile_job_roles').select('profile_id,job_role_id,is_primary').eq('profile_id',learnerId),
    db.from('profile_academy_roles').select('role_code,active').eq('profile_id',learnerId).eq('active',true)
  ]);
  if(peopleRes.error)throw peopleRes.error;
  if(rolesRes.error)throw rolesRes.error;
  if(linksRes.error)throw linksRes.error;
  managerDirectory=(peopleRes.data||[]).filter(p=>p.id!==learnerId);
  jobRoleDirectory=rolesRes.data||[];
  learnerJobRoles=linksRes.data||[];
  academyRoles=academyRes.error?[]:(academyRes.data||[]).map(x=>x.role_code);
};
function managerDisplayName(id){if(!id)return'Not assigned';const p=managerDirectory.find(x=>x.id===id);return p?.full_name||[p?.forename,p?.surname].filter(Boolean).join(' ')||'Unknown employee'}
function managerMeta(id){if(!id)return'';const p=managerDirectory.find(x=>x.id===id);if(!p)return'';return[p.job_title,p.employee_number?`Employee ${p.employee_number}`:''].filter(Boolean).join(' · ')}
function formatDob(value){if(!value)return'—';const[y,m,d]=String(value).split('-');return y&&m&&d?`${d}/${m}/${y}`:value}
function ensureManagerPanel(){let panel=document.getElementById('managerPanel');if(panel)return panel;const overview=document.getElementById('tab-overview'),stats=overview?.querySelector('.profile-grid');if(!overview||!stats)return null;panel=document.createElement('section');panel.id='managerPanel';panel.className='manager-panel';panel.innerHTML='<div class="manager-panel-head"><div><h2>Management & reporting</h2><p>A person can sit under up to three managers.</p></div><button class="btn secondary small" id="editManagersBtn" type="button">Edit managers</button></div><div class="manager-grid" id="managerGrid"></div>';stats.insertAdjacentElement('afterend',panel);panel.querySelector('#editManagersBtn').onclick=openProfileModal;return panel}
function renderManagers(){const panel=ensureManagerPanel();if(!panel)return;const grid=panel.querySelector('#managerGrid');const items=[['Primary manager',learner.manager_1_id],['Second manager',learner.manager_2_id],['Third manager',learner.manager_3_id]];grid.innerHTML=items.map(([label,id])=>`<div class="manager-card"><span>${esc(label)}</span><strong class="${id?'':'manager-none'}">${esc(managerDisplayName(id))}</strong>${id&&managerMeta(id)?`<small>${esc(managerMeta(id))}</small>`:''}</div>`).join('')}
function selectedPrimaryRole(){return learnerJobRoles.find(x=>x.is_primary)?.job_role_id||null}
function selectedSecondaryRole(){return learnerJobRoles.find(x=>!x.is_primary)?.job_role_id||null}
function jobRoleById(id){return jobRoleDirectory.find(r=>String(r.id)===String(id))}
function ensureJobRolePanel(){let panel=document.getElementById('jobRolePanel');if(panel)return panel;const managerPanel=ensureManagerPanel(),overview=document.getElementById('tab-overview');if(!overview)return null;panel=document.createElement('section');panel.id='jobRolePanel';panel.className='manager-panel';panel.innerHTML='<div class="manager-panel-head"><div><h2>Compliance job roles</h2><p>These roles drive this person’s compliance requirements.</p></div><button class="btn secondary small" id="editJobRolesBtn" type="button">Edit job roles</button></div><div class="manager-grid" id="jobRoleGrid"></div>';if(managerPanel)managerPanel.insertAdjacentElement('afterend',panel);else overview.appendChild(panel);panel.querySelector('#editJobRolesBtn').onclick=openProfileModal;return panel}
function renderJobRoles(){const panel=ensureJobRolePanel();if(!panel)return;const primary=jobRoleById(selectedPrimaryRole()),secondary=jobRoleById(selectedSecondaryRole());panel.querySelector('#jobRoleGrid').innerHTML=[['Primary job role',primary],['Secondary job role',secondary]].map(([label,role])=>`<div class="manager-card"><span>${esc(label)}</span><strong class="${role?'':'manager-none'}">${esc(role?.name||'Not assigned')}</strong>${role?`<small>${esc(role.division)}</small>`:''}</div>`).join('')}
const academyHierarchy=['trainee_assessor','assessor','iqa','lead_iqa'];
const jobTitles=['Senior Training Lead','Training Lead','Trainer','Assessor','IQA','Lead IQA','Operations Director','Operations Manager','Manager','Supervisor','Engineer','Fibre Supervisor','Fibre Engineer','Civils Supervisor','Civils Operative','Administrator','Finance Administrator','HR Administrator','Stores Operative'];
const academyLabels={none:'None',trainee_assessor:'Trainee Assessor',assessor:'Assessor',iqa:'IQA',lead_iqa:'Lead IQA'};
function academyRole(){for(let i=academyHierarchy.length-1;i>=0;i--)if(academyRoles.includes(academyHierarchy[i]))return academyHierarchy[i];return'none'}
function academyAdmin(){return academyRoles.includes('academy_admin')}
function jobTitleOptions(){const current=learner.job_title||'';const vals=[...new Set([current,...jobTitles].filter(Boolean))].sort((a,b)=>a.localeCompare(b));return'<option value="">Select job title</option>'+vals.map(v=>`<option value="${esc(v)}" ${current===v?'selected':''}>${esc(v)}</option>`).join('')}
function academyRoleOptions(){const cur=academyRole();return Object.entries(academyLabels).map(([v,l])=>`<option value="${v}" ${cur===v?'selected':''}>${l}</option>`).join('')}
function renderDateOfBirth(){const grid=document.getElementById('detailsGrid');if(!grid)return;let card=document.getElementById('dateOfBirthDetail');if(!card){card=document.createElement('div');card.className='detail';card.id='dateOfBirthDetail';const employeeCard=[...grid.querySelectorAll('.detail')].find(x=>(x.querySelector('span')?.textContent||'').trim().toLowerCase()==='employee number');if(employeeCard)employeeCard.insertAdjacentElement('afterend',card);else grid.appendChild(card)}card.innerHTML=`<span>Date of birth</span><strong>${esc(formatDob(learner.date_of_birth))}</strong>`}
function markCompactDetails(){document.querySelectorAll('#detailsGrid .detail').forEach(card=>{const label=(card.querySelector('span')?.textContent||'').trim().toLowerCase();card.classList.toggle('email-detail',label==='email');card.classList.toggle('id-detail',label==='learner id')})}
const originalRender=render;render=async function(){await originalRender();renderDateOfBirth();markCompactDetails();renderManagers();renderJobRoles();const g=document.getElementById('detailsGrid');if(g&&!document.getElementById('academyRoleDetail')){const d=document.createElement('div');d.className='detail';d.id='academyRoleDetail';g.appendChild(d)}const ad=document.getElementById('academyRoleDetail');if(ad)ad.innerHTML=`<span>Academy role</span><strong>${esc(academyLabels[academyRole()]||'None')}${academyAdmin()?' · Administrator':''}</strong>`};
function managerOptions(selected){const sorted=[...managerDirectory].sort((a,b)=>{const an=(a.surname||a.full_name||'').toLowerCase(),bn=(b.surname||b.full_name||'').toLowerCase();return an.localeCompare(bn)||String(a.forename||a.full_name||'').localeCompare(String(b.forename||b.full_name||''))});return'<option value="">Not assigned</option>'+sorted.map(p=>{const name=p.full_name||[p.forename,p.surname].filter(Boolean).join(' ')||'Unnamed employee',meta=[p.employee_number,p.job_title].filter(Boolean).join(' · ');return`<option value="${p.id}" ${selected===p.id?'selected':''}>${esc(name)}${meta?` — ${esc(meta)}`:''}</option>`}).join('')}
function jobRoleOptions(selected){const divisions=[...new Set(jobRoleDirectory.map(r=>r.division))];return'<option value="">Not assigned</option>'+divisions.map(division=>{const roles=jobRoleDirectory.filter(r=>r.division===division);return`<optgroup label="${esc(division)}">${roles.map(r=>`<option value="${r.id}" ${String(selected)===String(r.id)?'selected':''}>${esc(r.name)}</option>`).join('')}</optgroup>`}).join('')}
openProfileModal=function(){const primaryRole=selectedPrimaryRole(),secondaryRole=selectedSecondaryRole();modal('Edit person profile',`<div class="form-grid"><label>Forename<input name="forename" required value="${esc(learner.forename||'')}"></label><label>Surname<input name="surname" required value="${esc(learner.surname||'')}"></label><label>Date of birth<input name="date_of_birth" type="date" value="${esc(learner.date_of_birth||'')}"></label><label>Email<input name="email" type="email" value="${esc(learner.email||'')}"></label><label>Employee number<input name="employee_number" value="${esc(learner.employee_number||'')}"></label><label>Job title <span style="font-weight:500;color:#7a8491">(set by company)</span><select name="job_title">${jobTitleOptions()}</select></label><label>System role<select name="system_role"><option value="operative" ${(learner.organisational_access_role||learner.portal_access_role||'operative')==='operative'?'selected':''}>Operative</option><option value="supervisor" ${(learner.organisational_access_role||learner.portal_access_role)==='supervisor'?'selected':''}>Supervisor</option><option value="manager" ${(learner.organisational_access_role||learner.portal_access_role)==='manager'?'selected':''}>Manager</option><option value="operations_manager" ${(learner.organisational_access_role||learner.portal_access_role)==='operations_manager'?'selected':''}>Operations Manager</option></select></label><label>Organisation<input name="organisation" value="${esc(learner.organisation||'')}"></label><label>Account status<select name="account_status"><option value="active" ${learner.account_status==='active'?'selected':''}>Active</option><option value="archived" ${learner.account_status==='archived'?'selected':''}>Archived</option></select></label><div class="manager-fields-title">Academy role</div><div class="manager-select-note">Academy authority is separate from system access and compliance. Select the highest professional role attained.</div><label>Academy role<select name="academy_role">${academyRoleOptions()}</select></label><label>Academy administration<select name="academy_admin"><option value="no" ${academyAdmin()?'':'selected'}>No</option><option value="yes" ${academyAdmin()?'selected':''}>Yes</option></select></label><div class="manager-fields-title">Compliance job roles</div><div class="manager-select-note">Choose the main compliance role and, where required, a second role.</div><label>Primary job role<select name="primary_job_role">${jobRoleOptions(primaryRole)}</select></label><label>Secondary job role<select name="secondary_job_role">${jobRoleOptions(secondaryRole)}</select></label><div class="manager-fields-title">Management & reporting</div><div class="manager-select-note">Select up to three different managers.</div><label>Primary manager<select name="manager_1_id">${managerOptions(learner.manager_1_id)}</select></label><label>Second manager<select name="manager_2_id">${managerOptions(learner.manager_2_id)}</select></label><label>Third manager<select name="manager_3_id">${managerOptions(learner.manager_3_id)}</select></label></div>`,async f=>{const forename=f.get('forename').trim(),surname=f.get('surname').trim(),managers=[f.get('manager_1_id')||null,f.get('manager_2_id')||null,f.get('manager_3_id')||null],chosen=managers.filter(Boolean);if(new Set(chosen).size!==chosen.length)throw new Error('Please choose a different person for each manager position.');const primary=f.get('primary_job_role')?Number(f.get('primary_job_role')):null,secondary=f.get('secondary_job_role')?Number(f.get('secondary_job_role')):null;if(primary&&secondary&&primary===secondary)throw new Error('Primary and secondary job roles must be different.');const{error}=await db.from('profiles').update({forename,surname,full_name:`${forename} ${surname}`.trim(),date_of_birth:f.get('date_of_birth')||null,email:f.get('email').trim().toLowerCase()||null,employee_number:f.get('employee_number').trim()||null,job_title:(f.get('job_title')||'').trim()||null,organisational_access_role:f.get('system_role')||'operative',portal_access_role:f.get('system_role')||'operative',organisation:f.get('organisation').trim()||null,account_status:f.get('account_status'),manager_1_id:managers[0],manager_2_id:managers[1],manager_3_id:managers[2],updated_at:new Date().toISOString()}).eq('id',learnerId);if(error)throw error;const del=await db.from('profile_job_roles').delete().eq('profile_id',learnerId);if(del.error)throw del.error;const roleRows=[];if(primary)roleRows.push({profile_id:learnerId,job_role_id:primary,is_primary:true});if(secondary)roleRows.push({profile_id:learnerId,job_role_id:secondary,is_primary:false});if(roleRows.length){const ins=await db.from('profile_job_roles').insert(roleRows);if(ins.error)throw ins.error}const chosenAcademy=f.get('academy_role')||'none',admin=f.get('academy_admin')==='yes';const managed=[...academyHierarchy,'academy_admin'];const off=await db.from('profile_academy_roles').update({active:false}).eq('profile_id',learnerId).in('role_code',managed);if(off.error)throw off.error;const wanted=[];if(chosenAcademy!=='none'){const idx=academyHierarchy.indexOf(chosenAcademy);for(let i=0;i<=idx;i++)wanted.push(academyHierarchy[i])}if(admin)wanted.push('academy_admin');if(wanted.length){for(const role_code of wanted){const rr=await db.from('profile_academy_roles').upsert({profile_id:learnerId,role_code,active:true,granted_by:trainer.id},{onConflict:'profile_id,role_code'});if(rr.error)throw rr.error}const tm=await db.from('academy_training_team').upsert({profile_id:learnerId,active:true},{onConflict:'profile_id'});if(tm.error)throw tm.error}else{const tm=await db.from('academy_training_team').update({active:false}).eq('profile_id',learnerId);if(tm.error)throw tm.error}})};
const rewireProfileEditors=()=>{const top=document.getElementById('editProfileBtn'),details=document.getElementById('editProfileBtn2');if(top){top.onclick=null;top.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();openProfileModal()},true)}if(details){details.onclick=null;details.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();openProfileModal()},true)}};rewireProfileEditors();
(function installProfileShell(){
  if(document.body.dataset.profileShellReady==='1')return;document.body.dataset.profileShellReady='1';
  document.body.dataset.portalNav='academy';
  document.querySelector('.site-header')?.remove();
  let css=document.querySelector('link[href="portal-navigation.css"]');if(!css){css=document.createElement('link');css.rel='stylesheet';css.href='portal-navigation.css';document.head.appendChild(css)}
  const app=document.getElementById('profileApp'),errorBox=document.getElementById('profileError');if(!app)return;
  const shell=document.createElement('div');shell.className='profile-platform-shell';
  const side=document.createElement('aside');side.className='side';side.innerHTML='<div class="brand"><img src="rrta-logo.png" alt="RRTA"></div><div class="spacer"></div>';
  const main=document.createElement('main');main.className='profile-platform-main';
  const top=document.createElement('header');top.className='profile-platform-top';top.innerHTML='<strong>RRTA Training Academy Administration</strong><span>Person Profile</span>';
  const host=document.createElement('div');host.className='profile-platform-content';
  app.parentNode.insertBefore(shell,app);shell.append(side,main);main.append(top,host);host.appendChild(app);if(errorBox)host.appendChild(errorBox);
  const back=app.querySelector('.profile-topbar a.text-btn');if(back){back.href='manage-people.html';back.textContent='← Back To Manage People'}
  if(!document.querySelector('script[src="portal-navigation.js"]')){const script=document.createElement('script');script.src='portal-navigation.js';document.head.appendChild(script)}
})();