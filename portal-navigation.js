(function(){if(window.__rrtaPortalNavigationLoaded)return;window.__rrtaPortalNavigationLoaded=true;function boot(){const nav=window.RRTAPlatformNavigation,type=document.body.dataset.portalNav;if(!nav||!nav.menus[type])return;const side=document.querySelector(type==='learner'?'.sidebar':'.side');if(!side)return;const page=(location.pathname.split('/').pop()||'').toLowerCase(),icon=nav.icon;const active=href=>page===href.split('?')[0].split('#')[0].toLowerCase();const link=(label,ic,href,id='')=>`<a ${id?`id="${id}"`:''} class="portal-nav-link${active(href)?' active':''}" href="${href}">${icon(ic)}<span>${label}</span></a>`;const menu=nav.menus[type].map(item=>{if(item.type==='section')return `<div class="portal-nav-label">${item.label}</div>`;if(item.type==='link')return link(item.label,item.icon,item.href);if(item.type==='group')return `<div class="portal-nav-group"><button class="portal-nav-parent" type="button">${icon(item.icon)}<span>${item.label}</span><span class="portal-nav-chevron">›</span></button><div class="portal-nav-flyout">${item.items.map(([label,ic,href])=>link(label,ic,href)).join('')}</div></div>`;return''}).join('');const switches=type==='academy'?`${link('RRT Workforce Management','people','workforce.html')}${link('My Learning','course','learner-portal.html')}${link('Portal Home','dashboard','portal.html')}`:type==='workforce'?`${link('Academy Administration','course','academy-admin.html','academyLink')}${link('Portal Home','dashboard','portal.html')}`:`${link('Workforce Management','people','workforce.html','workforceAccessLink')}${link('Academy Administration','course','academy-admin.html','academyAccessLink')}${link('Portal Home','dashboard','portal.html')}`;side.querySelectorAll('.nav,.label,.side-title,.course-sub,.portal-switch,.portal-nav,.portal-nav-label,.portal-nav-group').forEach(n=>n.remove());const spacer=side.querySelector('.spacer'),account=side.querySelector('.account,.user-box'),logout=side.querySelector('#logout,#logoutBtn');const box=document.createElement('div');box.innerHTML=`<div class="portal-nav-label">${type==='academy'?'Academy Control Centre':type==='workforce'?'RRT Workforce Management':'RRT Learner Portal'}</div><nav class="portal-nav">${menu}</nav><div class="portal-switch"><nav class="portal-nav">${switches}</nav></div>`;const frag=document.createDocumentFragment();while(box.firstChild)frag.appendChild(box.firstChild);const anchor=spacer||account||logout;if(anchor)side.insertBefore(frag,anchor);else side.appendChild(frag);side.querySelectorAll('.portal-nav-parent').forEach(btn=>btn.onclick=e=>{e.stopPropagation();const g=btn.closest('.portal-nav-group'),open=!g.classList.contains('open');side.querySelectorAll('.portal-nav-group').forEach(x=>x.classList.remove('open'));if(open)g.classList.add('open')});side.querySelectorAll('.portal-nav-flyout a').forEach(a=>a.addEventListener('click',()=>side.querySelectorAll('.portal-nav-group').forEach(g=>g.classList.remove('open'))));document.addEventListener('click',e=>{if(!e.target.closest('.portal-nav-group'))side.querySelectorAll('.portal-nav-group').forEach(g=>g.classList.remove('open'))});side.querySelectorAll('a[href]').forEach(a=>a.addEventListener('click',e=>{const u=new URL(a.href,location.href);if(u.pathname===location.pathname&&u.hash&&typeof window.show==='function'){e.preventDefault();const view=u.hash.slice(1);window.show(view==='compliance'&&type==='learner'?'compliance':view);history.replaceState(null,'',u.hash)}}));}
const s=document.createElement('script');s.src='platform-navigation.js';s.onload=boot;document.head.appendChild(s)})();