(function(){if(window.__rrtaPortalNavigationLoaded)return;window.__rrtaPortalNavigationLoaded=true;function boot(){const nav=window.RRTAPlatformNavigation,type=document.body.dataset.portalNav;if(!nav||!nav.menus[type])return;const side=document.querySelector(type==='learner'?'.sidebar':'.side');if(!side)return;side.classList.add('portal-side');const shell=side.closest('.shell,.portal-shell');if(shell)shell.classList.add('portal-shell-standard');const current=new URL(location.href),page=(current.pathname.split('/').pop()||'').toLowerCase(),icon=nav.icon;const active=href=>{const target=new URL(href,location.href),targetPage=(target.pathname.split('/').pop()||'').toLowerCase();if(page!==targetPage)return false;if(target.search){for(const [k,v] of target.searchParams){if(current.searchParams.get(k)!==v)return false}}else if(current.search&&targetPage===page&&['courses.html','workforce-directory.html'].includes(page))return false;if(target.hash&&target.hash!==current.hash)return false;return true};const link=(label,ic,href,id='')=>`<a ${id?`id="${id}"`:''} class="portal-nav-link${active(href)?' active':''}" href="${href}">${icon(ic)}<span>${label}</span></a>`;const menu=nav.menus[type].map(item=>{if(item.type==='section')return `<div class="portal-nav-label">${item.label}</div>`;if(item.type==='link')return link(item.label,item.icon,item.href);if(item.type==='group'){const groupActive=item.items.some(([, ,href])=>active(href));return `<div class="portal-nav-group${groupActive?' current':''}" data-group="${item.label}"><button class="portal-nav-parent${groupActive?' active':''}" type="button">${icon(item.icon)}<span>${item.label}</span><span class="portal-nav-chevron">›</span></button><template class="portal-nav-template">${item.items.map(([label,ic,href])=>link(label,ic,href)).join('')}</template></div>`}return''}).join('');const switches=type==='academy'?`${link('My Learning','course','learner-portal.html')}${link('Portal Home','dashboard','portal.html')}`:type==='workforce'?`${link('Academy Administration','course','academy-admin.html','academyLink')}${link('Portal Home','dashboard','portal.html')}`:`${link('Workforce Management','people','workforce.html','workforceAccessLink')}${link('Academy Administration','course','academy-admin.html','academyAccessLink')}${link('Portal Home','dashboard','portal.html')}`;side.querySelectorAll('.nav,.label,.side-title,.course-sub,.portal-switch,.portal-nav,.portal-nav-label,.portal-nav-group').forEach(n=>n.remove());const spacer=side.querySelector('.spacer'),account=side.querySelector('.account,.user-box'),logout=side.querySelector('#logout,#logoutBtn');const box=document.createElement('div');box.innerHTML=`<div class="portal-nav-label">${type==='academy'?'Academy Control Centre':type==='workforce'?'RRT Workforce Management':'RRT Learner Portal'}</div><nav class="portal-nav">${menu}</nav><div class="portal-switch"><nav class="portal-nav">${switches}</nav></div>`;const frag=document.createDocumentFragment();while(box.firstChild)frag.appendChild(box.firstChild);const anchor=spacer||account||logout;if(anchor)side.insertBefore(frag,anchor);else side.appendChild(frag);let overlay=null,activeGroup=null;function closeFlyout(){if(overlay){overlay.remove();overlay=null}if(activeGroup){activeGroup.classList.remove('open');activeGroup=null}}function openFlyout(group,btn){closeFlyout();const tpl=group.querySelector('.portal-nav-template');if(!tpl)return;group.classList.add('open');activeGroup=group;overlay=document.createElement('div');overlay.className='portal-nav-global-flyout';overlay.innerHTML=tpl.innerHTML;document.body.appendChild(overlay);const r=btn.getBoundingClientRect();const maxTop=Math.max(10,window.innerHeight-overlay.offsetHeight-10);overlay.style.top=Math.min(r.top-4,maxTop)+'px';overlay.style.left=(r.right+10)+'px';overlay.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>closeFlyout()));}side.querySelectorAll('.portal-nav-parent').forEach(btn=>btn.onclick=e=>{e.stopPropagation();const g=btn.closest('.portal-nav-group');if(activeGroup===g){closeFlyout();return}openFlyout(g,btn)});document.addEventListener('click',e=>{if(!e.target.closest('.portal-nav-global-flyout')&&!e.target.closest('.portal-nav-group'))closeFlyout()});window.addEventListener('scroll',closeFlyout,true);window.addEventListener('resize',closeFlyout);side.querySelectorAll('a[href]').forEach(a=>a.addEventListener('click',e=>{const u=new URL(a.href,location.href);if(u.pathname===location.pathname&&u.hash&&typeof window.show==='function'){e.preventDefault();const view=u.hash.slice(1);window.show(view==='compliance'&&type==='learner'?'compliance':view);history.replaceState(null,'',u.hash);side.querySelectorAll('.portal-nav-link').forEach(x=>x.classList.remove('active'));a.classList.add('active')}}));if(type==='academy')cleanAcademyDashboard();}
function cleanAcademyDashboard(){if((location.pathname.split('/').pop()||'').toLowerCase()!=='academy-admin.html')return;const head=document.querySelector('.head p.muted');if(head)head.textContent='Manage RRT compliance, academy delivery and external customers from one control centre.';document.querySelectorAll('.workspace').forEach(card=>{const title=card.querySelector('strong');if(!title)return;const label=title.textContent.trim();if(label==='Workforce Administration'){card.href='compliance-operatives.html';title.textContent='Compliance Overview';const s=card.querySelector('span');if(s)s.textContent='See workforce compliance across RRT, then drill into people, teams, managers and job-role gaps.'}if(label==='Reports'){card.href='reports.html';const s=card.querySelector('span');if(s)s.textContent='Compliance, expiry, booking and training history reporting.'}});const panel=[...document.querySelectorAll('.panel')].find(x=>x.querySelector('h2')?.textContent.trim()==='Platform Boundary');if(panel){const h=panel.querySelector('h2');if(h)h.textContent='How The Platform Is Split';const p=panel.querySelector('p.muted');if(p)p.innerHTML='<strong>Academy Administration</strong> has full control. <strong>Workforce Management</strong> is the restricted manager view. <strong>Learner Portal</strong> is the individual self-service view.';}}
const s=document.createElement('script');s.src='platform-navigation.js';s.onload=boot;document.head.appendChild(s)})();